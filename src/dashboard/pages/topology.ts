/** Topology page — C1 ships a placeholder; real SVG topology lands in C5. */
import { renderPlaceholderPage } from './placeholders.js';
export function renderTopologyPage(): string { return renderPlaceholderPage('topology'); }
