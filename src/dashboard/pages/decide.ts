/** Decide page — C1 ships a placeholder; decision cards land in C4. */
import { renderPlaceholderPage } from './placeholders.js';
export function renderDecidePage(): string { return renderPlaceholderPage('decide'); }
