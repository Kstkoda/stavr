/** Toolkit page — C1 ships a placeholder; ESB + brick editor lands in C7. */
import { renderPlaceholderPage } from './placeholders.js';
export function renderToolkitPage(): string { return renderPlaceholderPage('toolkit'); }
