/** Streams page — C1 ships a placeholder; multi-pane terminal lands in C6. */
import { renderPlaceholderPage } from './placeholders.js';
export function renderStreamsPage(): string { return renderPlaceholderPage('streams'); }
