/** Plans page — C1 ships a placeholder; food-label rebuild lands in C3. */
import { renderPlaceholderPage } from './placeholders.js';
export function renderPlansPage(): string { return renderPlaceholderPage('plans'); }
