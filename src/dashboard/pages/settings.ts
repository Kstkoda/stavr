/** Settings page — C1 ships a placeholder; profile / scopes / no-go land in C9. */
import { renderPlaceholderPage } from './placeholders.js';
export function renderSettingsPage(): string { return renderPlaceholderPage('settings'); }
