/** Capabilities page — C1 ships a placeholder; Lego baseplate lands in C8. */
import { renderPlaceholderPage } from './placeholders.js';
export function renderCapabilitiesPage(): string { return renderPlaceholderPage('capabilities'); }
