---
name: job_status
tier: auto
category: worker
since: 0.7.0
stability: beta
---

# job_status

Full state of a single job by id or name. Auto-tier.

## Tier behaviour

AUTO — runs without confirmation. Read-only or has built-in escape hatches; the caller is responsible for using it intentionally.

## Input schema

```json
{
  "type": "object",
  "properties": {
    "id_or_name": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "id_or_name"
  ],
  "additionalProperties": false
}
```

## Output schema

```json
{
  "type": "object",
  "properties": {
    "job": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": {}
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "required": [
    "job"
  ],
  "additionalProperties": false
}
```

## Side effects

- read-only


## Error modes

- none — unknown id_or_name returns `{ job: null }`

## See also

- `job_list`

---

_This file is generated by `scripts/generate-tool-catalogue.ts`. Edit `src/tools/catalogue-data.ts` and re-run `npm run docs:tools`._
