---
name: github.list_labels
tier: auto
category: github-read
since: 0.1.0
stability: stable
---

# github.list_labels

List labels defined in a repo (name, color, description).

## Tier behaviour

AUTO — runs without confirmation. Read-only or has built-in escape hatches; the caller is responsible for using it intentionally.

## Input schema

```json
{
  "type": "object",
  "properties": {
    "repo": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "repo"
  ],
  "additionalProperties": false
}
```

## Output schema

```json
{
  "type": "object",
  "properties": {
    "labels": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": {}
      }
    }
  },
  "required": [
    "labels"
  ],
  "additionalProperties": false
}
```

## Side effects

- shells out to `gh label list --json` (read-only)


## Error modes

- gh CLI failure → `gh_failed`

## See also

- `github.add_labels`
- `github.remove_labels`

---

_This file is generated by `scripts/generate-tool-catalogue.ts`. Edit `src/tools/catalogue-data.ts` and re-run `npm run docs:tools`._
