---
name: github.read_commit
tier: auto
category: github-read
since: 0.1.0
stability: stable
---

# github.read_commit

Read commit detail (message, author, files changed, stats).

## Tier behaviour

AUTO — runs without confirmation. Read-only or has built-in escape hatches; the caller is responsible for using it intentionally.

## Input schema

```json
{
  "type": "object",
  "properties": {
    "repo": {
      "type": "string",
      "minLength": 1
    },
    "sha": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64
    }
  },
  "required": [
    "repo",
    "sha"
  ],
  "additionalProperties": false
}
```

## Output schema

```json
{
  "type": "object",
  "additionalProperties": {}
}
```

## Side effects

- shells out to `gh api repos/{repo}/commits/{sha}` (read-only)


## Error modes

- gh CLI failure → `gh_failed`

## See also

- `github.list_commits`

---

_This file is generated by `scripts/generate-tool-catalogue.ts`. Edit `src/tools/catalogue-data.ts` and re-run `npm run docs:tools`._
