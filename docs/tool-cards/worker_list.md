---
name: worker_list
tier: auto
category: worker
since: 0.1.0
stability: stable
---

# worker_list

List workers, optionally filtered by type or status. Auto-tier.

## Tier behaviour

AUTO — runs without confirmation. Read-only or has built-in escape hatches; the caller is responsible for using it intentionally.

## Input schema

```json
{
  "type": "object",
  "properties": {
    "type": {
      "type": "string"
    },
    "status": {
      "type": "string",
      "enum": [
        "starting",
        "running",
        "idle",
        "terminated",
        "crashed"
      ]
    }
  },
  "additionalProperties": false
}
```

## Output schema

```json
{
  "type": "object",
  "properties": {
    "workers": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "type": {
            "type": "string"
          },
          "cwd": {
            "type": "string"
          },
          "pid": {
            "type": "integer"
          },
          "status": {
            "type": "string",
            "enum": [
              "starting",
              "running",
              "idle",
              "terminated",
              "crashed"
            ]
          },
          "started_at": {
            "type": "string"
          },
          "ended_at": {
            "type": "string"
          },
          "last_activity_at": {
            "type": "string"
          },
          "metadata": {
            "type": "object",
            "additionalProperties": {}
          },
          "termination_reason": {
            "type": "string"
          },
          "exit_code": {
            "type": "integer"
          }
        },
        "required": [
          "id",
          "name",
          "type",
          "cwd",
          "status",
          "started_at",
          "metadata"
        ],
        "additionalProperties": false
      }
    }
  },
  "required": [
    "workers"
  ],
  "additionalProperties": false
}
```

## Side effects

- read-only orchestrator query


## Error modes

- none

## See also

- `worker_status`

---

_This file is generated by `scripts/generate-tool-catalogue.ts`. Edit `src/tools/catalogue-data.ts` and re-run `npm run docs:tools`._
